resolver: lts-20.11
packages:
- .
extra-deps: []

# The project relies on Cardano and Plutus libraries which are not part of the
# default resolver. You may need to provide local packages or additional
# repositories when setting up a full development environment.
